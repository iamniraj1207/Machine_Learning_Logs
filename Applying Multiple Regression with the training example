#The problem statement was there in the Juptyer Notebook of Andrew Ng course 
import numpy as np 
X_train = np.array([[2104, 5, 1, 45],[1416, 3, 2, 40],[852, 2, 1, 35]])#MULTIPLE FEATURES AND THEIR VALUES 
Y_train = np.array([460,232,178])#Price in DOLLARS

m = X_train.shape
print(m)
b_init = 785.1811367994083
w_init = np.array([ 0.39133535, 18.75376741, -53.36032453, -26.42131618])

def predict_single_loop(w,b,x):
    n = x.shape[0]
    p = 0
    for i in range(n):
        p_i = w[i]*x[i]
        p = p + p_i
    p = p + b
    return p


X_vec = X_train[0,:]    # selecting the ROW for the training set data 
F_wb = predict_single_loop(w_init,b_init,X_vec)
print(F_wb)

# Done with iterations 
        
